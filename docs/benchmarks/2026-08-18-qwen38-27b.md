# Qwen3.8-27B benchmark girişimi — 2026-08-18

**Sonuç: DEĞERLENDİRİLEMEDİ (operasyonel güvenilirlik FAIL). Öneri:
varsayılan model GLM 5.2 olarak kalmalı.**

## Denenen

`qwen/qwen3.8-27b` (OpenRouter; girdi $0.45/M, çıktı $3.20/M — GLM 5.2:
$1.19/M / $3.74/M). Kalıcı eval düzeneği (`scripts/eval_classifier.py`,
gold-500, gerçek üretim yolu) ile 4 ayrı koşu:

| # | Yapılandırma | Sonuç |
|---|---|---|
| 1 | parti 25, eşz. 4 (üretim ayarı) | Muhakeme (reasoning) çıktısı 240s zaman aşımını aşıyor → rotasyon tükendi |
| 2 | parti 10, eşz. 4 | Sağlayıcı 429 (Venice) → AllKeysExhausted |
| 3 | parti 10, eşz. 2 | Upstream "temporarily rate-limited" (AkashML 429 → Chutes 502) |
| 4 | parti 10, eşz. 2 (ertesi saatler) | Her çağrı iki anahtarda da 240s zaman aşımı, kesintisiz |

## Bulgular

1. Model **muhakemeli (reasoning)**: her yanıttan önce düşünce zinciri
   üretiyor; tek küçük istekte çalışıyor (sondajla doğrulandı) ama
   üretim boyutlu yapılandırılmış istekleri sağlayıcılar bugün
   taşıyamıyor.
2. Modelin OpenRouter sağlayıcı havuzu (Venice/AkashML/Chutes) sürekli
   kapasite sıkışması bildiriyor — bizim koddan bağımsız (hata metinleri
   upstream'den).
3. Nominal fiyat GLM'den düşük olsa da muhakeme token'ları çıktı
   maliyetine yazılır; eşit kalitede bile gerçek maliyet avantajı
   belirsiz olurdu.

## Karar

16k+ satırlık toplu işler dakikalarca süren, kesintisiz yüzlerce çağrı
gerektirir; 4/4 başarısız koşu bu modelin bugünkü servis kapasitesiyle
üretim yükümüzü taşıyamayacağını gösteriyor. Kalite karşılaştırması
(4 eksen) bu yüzden ölçülemedi. Sağlayıcı kapasitesi stabilleşirse
düzenek hazır: tek komutla yeniden denenebilir —

    docker exec imga-prod-api python /tmp/eval_classifier.py /tmp/gold4.csv \
      --tenant "NAVLUNGO TEST 2" --model "qwen/qwen3.8-27b" \
      --call-batch-size 10 --concurrency 2

Taban çizgisi (GLM 5.2, canlı prompt): duygu .970, kategori .908
(belirsiz .022), skor bandı .876 (komşu .996), deneyim .915.
