# Handoff: Sprint 8.3.3 Closure

**Tarih:** 2026-05-02 (local-agent)
**Sprint:** 8.3.3
**Yazar:** local-agent
**Hedef:** server-agent / claude-chat
**Durum:** resolved
**Öncelik:** normal

> Server-agent kendi authoritative versiyonunu üretene kadar geçici stub. Sprint 8.3.1 / 8.3.2'de aynı pattern: server'ın detaylı closure patch'i lokal stub'ı override edecek.

## Bağlam

Sprint 8.3.3 (gelişmiş analitik + /insights sayfası) production'a deploy edildi. İlk push'ta 6 endpoint 500 döndü (`slots=True` dataclass + `__dict__` çakışması), Round 1 fix `7b12c1a` ile kapandı. Browser smoke iki UX bug çıkardı (tab nav refresh sonrası, date picker -1 gün); ikisi de tek kök neden olan ISO datetime URL param'ına düşüp Round 2 fix `fde7575` ile kapandı.

## Sprint 8.3.3 — Tamamlanan kapsam

### Backend
- `AnalyticsService` (`services/analytics_service.py`) — 7 frozen-dataclass aggregation: sentiment_distribution, category_distribution, sentiment_by_category (heatmap matrix), override_stats (placeholder), sentiment_timeline (day/week/month), ticket_resolution_time (avg/median/p95 + buckets), sensitivity_distribution.
- `tenant_analytics.py` — 7 GET endpoint, hepsi RLS-bound. `dataclasses.asdict()` projection (Round 1 fix).
- 7 endpoint hepsi 200 OK (Round 1 sonrası), CSV-encoded querystring filtreleri (`/tickets/stats` + `/reviews` ile uyumlu).

### Frontend
- 7 TanStack Query hook + shared `qs()` + `commonParams()`/`dateRangeParams()` helpers (Round 2 ile YYYY-MM-DD URL → local-midnight ISO API conversion).
- Custom SVG heatmap component (~200 satır, no @nivo).
- `/insights` 5-tab page (Duygu / Kategori / Çapraz Analiz / Override Katmanları / Biletler), URL state `usePathname() + router.push` kanonik pattern (Round 2), FilterBar YYYY-MM-DD inputs.
- `<ChartFrame>` shared error/loading/empty + `min-h-[Npx]` chart wrapper (Round 1).
- Sidebar "İçgörüler" linki.

### Tests
- `test_analytics.py` — 8 entegrasyon testi (aggregation correctness × 6 + RLS isolation + empty-shape regression). Toplam **54/54 pass**.

## Smoke fix bug'ları

- `7b12c1a` (Round 1 — KRİTİK) — `tenant_analytics.py` `__dict__` → `asdict()` (slots dataclass)
- `1d4c8e9` (Round 1) — `<ChartFrame>` error/loading/empty + chart min-h, FilterBar local-state + commit-on-blur
- `fde7575` (Round 2) — URL date param YYYY-MM-DD, `usePathname() + router.push` kanonik pattern, hooks `dateOnlyToLocalIso()` ile API'ye ISO datetime gönderiyor

## Production smoke test sonucu

- ✅ /insights sayfası: 7 endpoint 200, charts render
- ✅ Sekme geçişi (refresh sonrası dahil)
- ✅ Date picker: 04.04.2026 seç → blur → 04.04.2026 kalıyor
- ✅ Heatmap cell click → `/reviews?primary_category=X&sentiment_labels=Y` filtrelenmiş listeye atlıyor
- ✅ Filter bar: tarih aralığı seçildiğinde tüm tab'lar yeniden fetch

## Test stack durumu

**54 passed / 0 failed** (test compose, live Postgres, 13.88s).

## Bilinen eksikler (Sprint 8.3.4'e ertelendi)

- `Review.overrides_applied` JSONB doldurma + override-stats gerçek veri
- Dashboard home page widget'ları
- /reviews/[id] full detail layout
- Performance gate (5K/50K xlsx + 1K/10K batch — `@pytest.mark.slow`)
- Heatmap mobile responsive iyileştirmesi
- /insights PDF export

## Commit zinciri (Sprint 8.3.3)

- `392358d` feat(api) — AnalyticsService 7 aggregation
- `ec7af84` feat(api) — 7 GET endpoint + main.py register
- `6f7a28d` test(api) — 8 entegrasyon testi
- `5eadc0f` feat(web) — types + 7 hooks
- `ee6dcc0` feat(web) — custom SVG heatmap
- `21b6c58` feat(web) — /insights 5-tab page
- `764f1c8` feat(web) — sidebar nav
- `8c2bd83` docs(handoff) — open spec
- `7b12c1a` fix(api) — `__dict__` → `asdict()` (Round 1, KRİTİK)
- `1d4c8e9` fix(web) — error states + chart min-h (Round 1)
- `ede2c97` docs(handoff) — smoke-fixes open
- `fde7575` fix(web) — tab nav + date picker timezone (Round 2)
- `08165bc` docs(handoff) — smoke-fixes Round 2 append

## Süreç notu

Round 1 kök sebebi: lokalde sadece ruff + mypy + tsc koşturuldu, pytest atlandı. Sprint 8.3.4'ten itibaren backend push öncesi test compose ZORUNLU.

## Cevap

Sprint 8.3.3 KAPANDI. Sprint 8.3.4 (override görünürlüğü final + reviews detail + dashboard genişleme + ertelenen işler) handoff'u Claude Chat'ten alındı, implementasyon başladı.

---
