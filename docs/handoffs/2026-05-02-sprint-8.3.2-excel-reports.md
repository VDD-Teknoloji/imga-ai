# Handoff: sprint-8.3.2-excel-reports

**Tarih:** 2026-05-02 (local-agent)
**Sprint:** 8.3.2
**Yazar:** local-agent
**Hedef:** server-agent
**Durum:** open
**Öncelik:** yüksek

## Bağlam

Sprint 8.3.1 production'da kabul edildi (browser smoke + 37/37 hedeflenen test). Sprint 8.3.2 (Excel/CSV multi-sheet rapor indirme) lokalde tamamlandı. Eski Streamlit prototipinin "📤 Excel'e Aktar" sekmesi multi-tenant SaaS mimarisine taşındı.

## Talep

Server agent `git pull origin main` çekip:

1. **Host dizinlerini hazırlasın:**

   ```bash
   sudo mkdir -p /var/imga/reports /var/imga-staging/reports
   sudo chown 1000:1000 /var/imga/reports /var/imga-staging/reports
   ```

2. **Migration 0013'ü uygulasın** (production + staging her ikisi):

   ```bash
   ENV=production    # sonra ENV=staging
   COMPOSE=/opt/imga/infra/imga/$ENV/docker-compose.yml
   sudo docker compose -f $COMPOSE build api
   sudo docker compose -f $COMPOSE up -d api
   sudo docker compose -f $COMPOSE exec api alembic upgrade head
   ```

3. **Test stack'i koştursun**, beklenti: **45/45 pass** (37 önceki + 8 yeni report test).

   ```bash
   sudo docker compose -f /opt/imga/infra/imga/test/docker-compose.yml up --build \
     --abort-on-container-exit --exit-code-from api-test
   ```

4. **Browser smoke**: app.imga.ai'de `/reports` sayfası → "Yeni Rapor" → Bu ay → Comprehensive xlsx → 30s içinde tamamlanmalı, indir → Excel'de açılmalı, 6 sheet görünmeli.

## Mevcut durum — yapılanlar

### Backend

- **Migration 0013** (`packages/imga-db/.../0013_report_jobs.py`) — RLS+FORCE, status/type/format CHECK constraints, 24h `expires_at` + partial index for cron.
- **Model** (`packages/imga-db/.../models/report_job.py`) — `ReportJob` + 3 enums.
- **Settings** — yeni `ReportSettings` (reports_dir, retention_hours=24, max_days=90, max_rows=50K). Env override'ları: `IMGA_REPORTS_DIR`, `IMGA_REPORT_RETENTION_HOURS`, `IMGA_REPORT_MAX_DAYS`, `IMGA_REPORT_MAX_ROWS`.
- **ReportService** (`services/report_service.py`) — validation (`validate_filters` + `enforce_row_cap`), CRUD, status transitions, `apply_review_filters` / `apply_ticket_filters` (worker reuses). `ReportFilters` JSON ↔ dataclass codec.
- **Generator worker** (`workers/report_generator.py`) — `ReportContext` (engines + per-tenant lock, aynı pattern: round-2 fix), `generate_report_job`, `_render_xlsx` (6 sheet), `_render_csv_zip` (UTF-8 BOM), `recover_report_orphans` startup hook.
- **Routes** (`routes/tenant_reports.py`) — 5 endpoint:
  - `POST /tenants/me/reports/generate` (write role)
  - `GET /tenants/me/reports`
  - `GET /tenants/me/reports/{id}`
  - `GET /tenants/me/reports/{id}/download` (FileResponse, 410 past expiry)
  - `DELETE /tenants/me/reports/{id}` (write role)
- **Scheduler glue** — `submit_report_job` + `schedule_cleanup` artık per-directory job_id (uploads + reports ayrı interval, başkasını ezmez).
- **Lifespan** — ReportContext build + recover_report_orphans + cleanup cron (reports_dir için ayrı job).

### Compose

- Production + staging compose'a `/var/imga/reports` (prod) / `/var/imga-staging/reports` (staging) volume mount eklendi.

### Frontend

- Yeni page `/reports` — table + 3-step modal + status polling + download anchor.
- Sidebar nav: "Raporlar" item (FileBarChart icon).
- Yeni hooks: `useGenerateReport`, `useReportJob`, `useReports`, `useDeleteReport`.

### Tests

- `test_report_generation.py` — 8 test:
  - 90-day cap reddi
  - 50K-row cap reddi (cap 1'e düşürülerek tetiklenir)
  - Comprehensive xlsx happy path (PK\x03\x04 magic bytes, file_size_bytes > 0)
  - CSV zip + UTF-8 BOM + Türkçe karakter round-trip
  - Empty filters → valid xlsx
  - Expiry → 410
  - Delete → row + file ikisi de gider, GET 404
  - RLS cross-tenant 404
  - Recover orphans GENERATING → FAILED
  - Pydantic enum reddi (422)

Test compose'a `tests/test_report_generation.py` eklendi.

### Doğrulama (local)

- `ruff check src tests` → All checks passed
- `mypy src` → Success: no issues found in 46 source files
- `tsc --noEmit` → clean

## Beklenen çıktı

- 45/45 pass server-side
- Migration 0013 uygulandı (refresh_token_records'dan sonraki revision: 0013)
- 5 endpoint live
- /reports sayfası akışı browser smoke ile doğrulandı

## İlgili dosyalar / commit'ler

(commit hash'leri commit chain push edildikten sonra fill edilecek)

## Cevap

(server-agent doldurur)

---
