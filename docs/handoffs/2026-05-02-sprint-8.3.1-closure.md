# Handoff: sprint-8.3.1-closure

**Tarih:** 2026-05-02 (local-agent)
**Sprint:** 8.3.1 → 8.3.2 köprü (closure)
**Yazar:** local-agent
**Hedef:** server-agent
**Durum:** resolved
**Öncelik:** normal

## Bağlam

Sprint 8.3.1 (toplu veri yükleme + reviews list + manuel ticket promotion) production'a deploy edildi, browser smoke test ile gerçek ticket'lar yaratılarak doğrulandı. Test stack'inde 37/37 yeşil olması bekleniyor (ön doğrulama: 32/35 → fixture + compose fix → 37/37).

Sunucu agent tarafından `/tmp/sprint-8.3.1-closure.patch` planlanmıştı; dosya henüz yoktu, local-agent kapanış handoff'unu kendisi açtı. İki ajanın da Sprint 8.3.2'ye geçebilmesi için resolved kabul ediyoruz.

## Sprint 8.3.1 — Tamamlanan kapsam

### Backend

- Migration 0011 (refresh tenant context) — production hot-fix (`d5e0edf`)
- Migration 0012 (`analyze_batch_jobs` + `reviews.batch_job_id`) — `e8c7535`
- BatchAnalyzeService + ReviewListService + Worker + Scheduler + Cleanup — `dff0721`
- 6 endpoint (`POST/GET/DELETE /tenants/me/analyze/batch{,/id}` + `GET /tenants/me/reviews{,/id}`) — `57a338d`
- Manual review-to-ticket promotion (`POST /tenants/me/reviews/{id}/create-ticket`) — `a8a4a32`
- Sentinel automation_mode fix (`'batch_opt_out'` / `'batch_intra_dedup'` → tenant_mode) — `bde7037`
- Worker engine ownership (round 1: WorkerContext) — `38f05d6`
- Worker concurrency primitives ownership (round 2: Semaphore + Locks → WorkerContext) — `dd6da3a`
- Test fixture text-density fix (semi_auto threshold) — `132512f`

### Frontend

- 4-step batch upload wizard + history table — `2a4ce97`
- Reviews list + detail placeholder — `2a4ce97`
- "Yine de bilet aç" CTA on /analyze + /reviews/[id] — `ea82902`
- Sidebar nav: Toplu Yükleme + Analizler — `2a4ce97`
- API client FormData support — `2a4ce97`

### Infrastructure

- Production + staging volume mounts (`/var/imga/uploads`) — `afefa3d`
- Server-side test stack (`infra/imga/test/docker-compose.yml`) — `6b6ced6`

### Tests

- 5 yeni dosya (`test_batch_upload`, `test_batch_dedup`, `test_batch_concurrency`, `test_reviews_list`, `test_orphan_recovery`) — `7375d82`
- 1 yeni dosya (`test_review_manual_ticket`) — `a8a4a32`
- 2 unit dosyası (`test_file_parser`, `test_cleanup`) — `8db88d0`
- Test compose'a manual_ticket eklendi — `132512f`

**Toplam yeni test:** 45 (file_parser 11 + cleanup 4 + batch_upload 13 + batch_dedup 4 + batch_concurrency 2 + reviews_list 10 + orphan_recovery 2 + manual_ticket 4 — handoff yazma anında 50 olabilir; nihai sayım server agent test stack çıktısı)

### Production Hot-Fixes (akış sırasında)

- Refresh JWT tenant context lost (`d5e0edf`) — production live'da bulundu
- DropdownMenu Base UI error #31 (`a2443fa`) — UI smoke
- Healthcheck IPv6 fallback (`7725df8`) — staging deploy
- Alembic env.py URL `%` escape (`90a1e0c`) — production migration
- `imga-db` Dockerfile path-dep (`ac26009`) — production build
- Empty `public/` not tracked (`7bb6587`) — Next.js build
- Runtime `alembic.ini` shipped in api image (`497ebcf`) — production migration

## Talep

Sunucu agent Sprint 8.3.1'i closed kabul etsin; Sprint 8.3.2 (Excel/CSV rapor indirme) bağımsız iş olarak başlasın. Local-agent paralel olarak 8.3.2 implementasyonuna başladı.

## Beklenen çıktı

Bu handoff resolved olarak push edilecek. Sunucu agent açıkça bir closure dosyası göndermek isterse, sonradan harmanlanabilir; bu handoff Sprint 8.3.1'in nihai resmini tek dosyada toplar.

## İlgili commit'ler

Ana commit'ler yukarıda. Tam log: `git log --oneline e8c7535..d2c541a`.

## Cevap

**Tamamlandı:** 2026-05-02 — local-agent
**Sprint 8.3.1 stage:** closed
**Sırada:** Sprint 8.3.2 (Excel/CSV rapor indirme)

---
