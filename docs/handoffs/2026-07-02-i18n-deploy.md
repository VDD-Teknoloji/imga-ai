# Deploy Prompt — İmga i18n (TR/EN, kurum-güdümlü) · api + web + migration

**Tarih:** 2026-07-02 · **Yazar:** local-agent → server-agent · **Hedef commit:** `5f3f567` (main HEAD)
**Kapsam:** Bu deploy HEM `api` HEM `web` image'ını etkiler (frontend i18n). **YENİ MIGRATION: 0035.**

## Ne geliyor
- **Backend** (`234490d`): `tenants.language` kolonu (migration 0035, CHECK tr/en, mevcut kurumlar 'tr' backfill). Süper-yönetici tenant create/update `language` alır. `/auth/me` + `available_tenants` aktif kurumun dilini açığa çıkarır. AI çıktı dili: SWOT / OKR / Yönetici Brifing → EN kurumda İngilizce üretir (`language_directive`, DB-override promptlar dahil).
- **Frontend** (`5f3f567`): tam i18n (~1300 anahtar) — login + panel + analitik + ticket + veri yükleme + ayarlar + admin + navigasyon. Locale kurum-güdümlü (öncelik: kurum dili › pre-auth tercih › tarayıcı dili); tenant değişince UI dili döner. Tenant-oluşturmada dil seçici. Paylaşılan enum badge'ler (durum/öncelik/rol/plan) locale-farkında. Pre-auth TR/EN toggle + `<html lang>` senkronu.

**Doğrulama (local):** backend `:5433` → **645 passed, 2 skipped** (migration 0035 uygulandı); frontend `tsc --noEmit` → **0 hata**.

## Deploy (aynı stack; api + web birlikte)
```bash
cd /opt/imga && git pull origin main            # 5f3f567
COMPOSE=/opt/imga/infra/imga/production/docker-compose.yml
sudo docker compose -f $COMPOSE build api web    # HEM api HEM web (web build ağır — Next build)
sudo docker compose -f $COMPOSE up -d api web
# *** MIGRATION ŞART (0034 -> 0035) ***
sudo docker compose -f $COMPOSE exec api alembic upgrade head
```

## Doğrulama (deploy sonrası smoke)
1. **Migration:** `sudo docker compose -f $COMPOSE exec postgres psql -U imga_owner -d imga -c "SELECT slug, language FROM tenants LIMIT 5;"` → mevcut kurumlar `tr`.
2. **Kurum dili seçimi:** Süper-yönetici → Yönetim → Kurumlar → **Yeni kurum** → "Kurum dili" = **English** → oluştur. O kuruma geçince (tenant switch) panel **İngilizce** olmalı.
3. **/me:** `GET /auth/me` yanıtında `active_context.language` + her `available_tenants[].language` dolu.
4. **Pre-auth toggle:** `/login` sağ üst **TR/EN** düğmesi arayüz dilini anında değiştirir (tercih kalıcı).
5. **AI dili:** EN kurumda bir SWOT/OKR/Brifing üret → çıktı **İngilizce** (Gemini anahtarı gerekir).
6. `<html lang>` aktif dile göre (`tr`/`en`) güncelleniyor.

## Rollback
- Migration **additive** — `alembic downgrade 0034` kolonu güvenle düşürür (varsayılan davranış Türkçe).
- Kod: `git revert 5f3f567 234490d` → `build api web` → `up -d`. UI Türkçe'ye, AI Türkçe'ye döner.

## Bilinen minör kalanlar (aynı desenle kademeli — deploy engelleyici DEĞİL)
- Birkaç settings alt-bileşeni (automation-mode-form, category-toggle-list, custom-categories-section), `SMART_FIELD_LABELS` (upload dropdown), ticket aksiyon-buton etiketleri (Üstlen/Çöz…), Türkçe tarih/relative-time formatı — EN kurumda bu noktalarda hâlâ Türkçe görünebilir; `t()` + `localeLabels` deseniyle sonraki turda tamamlanır.
- Kurum dilini SONRADAN değiştirme: backend `update_settings` `language` alıyor; şu an yalnız süper-yönetici admin panelinden. Tenant-admin'in kendi Ayarlar'ından değiştirmesi ayrı bir dilim.

## Raporla
Migration çıktısı + smoke sonuçları (özellikle EN kurumda panel dili + AI çıktı dili). Kırmızı olursa tam çıktı → local-agent düzeltir.
