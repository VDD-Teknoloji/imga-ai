# Handoff: Sprint 9.0 Performance Baseline

**Tarih:** 2026-05-02
**Sprint:** 9.0 (planning)
**Yazar:** server-agent
**Hedef:** claude-chat
**Durum:** open
**Öncelik:** normal

## Bağlam

9699 satırlık production batch upload (job `09834da5-deba-4497-bdbc-2ec0518dcb5e`) çalışırken read-only ölçüm penceresi açıldı. Sprint 8.3.x kapanıyor; Sprint 9.0 (performans) için "before" baseline'ı bu ekran görüntüsünden gelir.

## Baseline ölçümleri (Sprint 8.3.x sonu)

### Worker resource (canlı job sırasında, 2 snapshot)

| Snapshot | CPU % | MEM GiB | NET I/O | BLOCK I/O | PIDS |
|---|---|---|---|---|---|
| t+1h45m | 217.18% | 1.091 / 3.0 | 4.24MB / 4.52MB | 670MB / 1.04MB | 34 |
| t+1h46m | 260.28% | 1.055 / 3.0 | 4.24MB / 4.52MB | 670MB / 1.04MB | 34 |

- CPU iki çekirdek + payına yakın (BERT inference CPU-bound, GPU yok)
- Memory rahat (~1.1 GiB), 3 GiB limitin %35'i — şu an memory bottleneck değil
- Network minimal — tüm trafik containers arası
- Disk I/O 670MB read (BERT model + Hugging Face cache; bake edilmiş ama warm-up sayfaları)

### Batch processing hızı

- **Throughput:** ~0.91 sn/satır (7000 satır 6358 sn'de) → final job 0.91 s/row sabit kaldı
- **Total süre (9699 satır):** 2h 27m 34s
- **Chunk size:** 1000 satır
- **Commit interval:** ~15-17 dk (chunk sonu DB write)
- Confirmed alive: chunk 6→7 commit gözlendi (6000→7000)

### Tahmini bottleneck breakdown

Instrumentation yok — şu an SADECE container CPU profili var. Granular ölçüm için:

| Bileşen | Tahmin | Doğrulama |
|---|---|---|
| BERT inference (`model.forward`) | %75-85 | py-spy / cProfile gerek |
| Override pipeline (kategori + override layers) | %5-15 | aynı |
| DB write (chunk commit) | %2-5 | pg_stat_statements gerek |
| Network/serialization | %2-5 | aynı |

### Postgres profiling

- **`pg_stat_statements` extension YÜKLENMEMİŞ** (`relation "pg_stat_statements" does not exist`).
- Sprint 9.0'ın ilk işi: `CREATE EXTENSION pg_stat_statements;` migration veya init script güncellemesi. Init script PG image baseline'a `shared_preload_libraries = 'pg_stat_statements'` parametresini de eklemeli (postgres.conf veya `command:` override).

### Application log instrumentation

- **Tek log:** `batch worker: starting job <id> (tenant <id>)` — start anında.
- Chunk-level log YOK. Inference timing YOK. DB commit timing YOK.
- 1h45m boyunca worker'dan tek satır bile yeni log gelmedi (kullanıcının "donmuş gibi" hissetmesinin teknik kaynağı).
- Sprint 9.0 ilk adım: `chunk N/M processed in Xs (BERT Ys, override Zs, write Ws)` formatında structured log.

### Frontend etkisi (gözlem)

- `docker stats` ve DB sorgusu canlı; ama frontend "Yükleniyor..." takılı.
- React Query default 3 retry × exponential backoff → ~30 sn pencerede yanıt yoksa pasif düşüyor; 17dk commit interval'ı bu pencereyi katlayarak aşıyor.
- Hard refresh + `/analyze/upload/history` workaround'ı çalışıyor.

## Sprint 9.0 hedefler (tartışılacak)

| Metrik | Mevcut | Hedef | Çarpan |
|---|---|---|---|
| Tek satır | ~1 sn | <100 ms | 10x |
| 1000 satır chunk | ~1000 sn | <100 sn | 10x |
| 10K satır batch | ~2.7 saat | <20 dk | 8x |

## Yapılması önerilen işler

### Profiling önce, optimizasyon sonra
1. **`pg_stat_statements` extension** — Postgres init script'e `CREATE EXTENSION` ekle, container restart'ta gelsin.
2. **Worker chunk-level structured log** — bottleneck breakdown'ı log'tan çıkarılabilir hale getir.
3. **py-spy / cProfile entegrasyonu** — geliştirici makinesinde bir batch koşumu için flame graph üret.

### BERT optimizasyonu
4. **BERT batch_size** (şu an 32, modele göre 64-128 daha hızlı olabilir — RAM ↔ throughput trade-off).
5. **Model warm-up cache** — ilk satır cold-start jitter'ı (zaten image'a bake edildi ama mmap pages cold).
6. **Mixed precision (FP16)** — CPU'da etkisi sınırlı, ama torch CPU AMP destekli; deneyle.
7. **ONNX export** — savasy/bert-base-turkish modelini ONNX'e çevir, ONNX Runtime ile %2-4x CPU hızlanma.

### Override pipeline
8. **Erken çıkış** — kategori match yoksa override layers'ı atla.
9. **Cache** — tenant config TTLCache zaten var; kategori-level cache eklenebilir.

### DB / I/O
10. **Bulk INSERT yerine COPY** — chunk commit'inde 1000 satır COPY ile gönder.
11. **Connection pool sizing** — `pool_size` + `max_overflow` worker'ın paralel chunk'ları için tune et.

### UX
12. **Chunk size 1000 → 100** — her ~100 sn'de UI update'i, daha responsive feel.
13. **Mid-chunk progress field** — DB'ye chunk içinde `processed_rows` partial update (transaction commit'siz).
14. **Frontend stale-after-N-fails state** — "Backend hâlâ çalışıyor, son güncelleme N dk önce" mesajı.

### Mimari
15. **GPU host opsiyonu** — 10x+ hızlanma için en hızlı yol; ama maliyet + ops karmaşıklığı (separate VPS, model swap).
16. **Job kuyruğu external (Redis + RQ/Celery)** — şu an APScheduler tek instance; multi-worker için altyapı.

### Observability / hata izleme (eklendi 2026-05-04, Sprint 8.3.6.6 round-3 sonrası)

**API error handler `logger.exception()` zorunlu pattern** — Sprint 9.0 çalışmasından önce halledilmeli.

- **Bağlam:** Sprint 8.3.6.6'da SWOT generate üç round debug istedi (round-1 `minItems`, round-2 `enum without type`, round-3 yeni cür'et). Production'da 500 dönüyordu ama backend log'da traceback YOKTU — root cause her seferinde Gemini API'sinin error response body'sini ayrıca yakalayan SDK katmanına `print()` eklenerek bulundu.
- **Minimum gereken:** Tüm 5xx path'lerinde `logger.exception(...)` (otomatik traceback) + structured fields: `request_id`, `tenant_id`, `user_id`, `exception_type`, route path. FastAPI exception handler middleware ile tek noktada uygulanmalı; tek tek route'ta tekrarlanmasın.
- **Sentry (Sprint 8.6) öncesi:** Sentry SDK'sı tracebacks'i otomatik alır ama o entegrasyon gelene kadar bile minimum stdout'a `logger.exception` çıkması Sprint 9.0 çalışmasının hata-payını öder.
- **Doğrulama testi:** route layer test'inde mock provider 500 fırlatsın → caplog'da traceback satırı bekle.

## Cevap

(claude-chat dolduracak — Sprint 9.0 spec'i hazırlanırken bu baseline referans olarak kullanılacak)

---
